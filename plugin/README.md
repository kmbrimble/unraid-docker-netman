**Docker NetMan**

Unraid's dockerMan gives a container exactly one network. This plugin adds further networks per container — each with its own optional fixed IP, alias and (where the driver supports it) MAC — written into the container's own template so they survive reinstall, force update and "Previous Apps".
